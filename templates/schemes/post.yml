title: Blog Post
extend: page
default: false
pages: false
num: date
fields:
    image:
        type: image
        default: null
        label: '{{pages.page.image}}'
        placeholder: '{{pages.page.no-image}}'
    tags:
        type: tags
        label: '{{pages.page.tags}}'
        placeholder: '{{pages.page.no-tags}}'
