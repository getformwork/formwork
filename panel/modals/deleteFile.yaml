title: '{{panel.pages.deleteFile}}'

message: '{{panel.pages.deleteFile.prompt}}'

buttons:
    dismiss:
        action: dismiss
        icon: times-circle
        label: '{{panel.modal.action.cancel}}'
        variant: secondary

    delete:
        action: submit
        icon: trash
        label: '{{panel.modal.action.delete}}'
        align: right
        variant: danger
