title: '{{panel.pages.changes.detected}}'

message: '{{panel.pages.changes.detected.prompt}}'

buttons:
    dismiss:
        action: dismiss
        icon: times-circle
        label: '{{panel.modal.action.cancel}}'
        variant: secondary

    continue:
        action: command
        icon: exclamation-circle
        label: '{{panel.modal.action.continue}}'
        align: right
        command: continue
